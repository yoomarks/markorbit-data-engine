name: Production E Backup Functional Recovery Provenance Runtime

on:
  pull_request:
    paths:
      - 'scripts/preflight-production-e-backup-functional-recovery-provenance.ps1'
      - 'tests/test_production_e_backup_functional_recovery_provenance_contract.py'
      - '.github/workflows/production-e-backup-functional-recovery-provenance-runtime.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  windows-powershell51:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Parse and exercise E backup functional provenance under Windows PowerShell 5.1
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $scriptPath = Join-Path $PWD 'scripts\preflight-production-e-backup-functional-recovery-provenance.ps1'
          $tokens = $null
          $errors = $null
          $ast = [System.Management.Automation.Language.Parser]::ParseFile($scriptPath, [ref]$tokens, [ref]$errors)
          if ($errors.Count -ne 0) {
            $errors | ForEach-Object { Write-Host $_.Message }
            throw 'E backup functional provenance failed Windows PowerShell 5.1 parse.'
          }

          $paramNames = @($ast.ParamBlock.Parameters | ForEach-Object { $_.Name.VariablePath.UserPath })
          foreach ($forbidden in @('Apply','Resume','AcknowledgeDelete','AcknowledgeMove','AcknowledgeVhdxMutation')) {
            if ($paramNames -contains $forbidden) { throw "Functional provenance must not expose destructive parameter: $forbidden" }
          }

          powershell.exe -NoProfile -ExecutionPolicy Bypass -File $scriptPath `
            -ExpectedMainSha $env:GITHUB_SHA `
            -AcceptedEBackupPreflightReceiptPath 'C:\fixture\unused.json' `
            -ContractOnly
          if ($LASTEXITCODE -ne 0) { throw "ContractOnly exited $LASTEXITCODE." }

          Write-Host 'PRODUCTION_E_BACKUP_FUNCTIONAL_RECOVERY_PROVENANCE_CONTRACT_DIRECT_INVOCATION_OK'
