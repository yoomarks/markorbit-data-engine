# MarkOrbit Data Engine M1.5.1 Hotfix

修复 ClickHouse 24.8 SQL alias resolution issue.

原因:
ClickHouse 会在同一 SELECT 中将 alias source_start_line 替换到 argMin/argMax 参数，
导致 aggregate inside aggregate 错误。

修复:
所有 argMin/argMax 的排序字段改为 toUInt64(source_start_line)，避免 alias 捕获。

不需要 reset 数据库。
